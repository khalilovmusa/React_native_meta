import { StyleSheet } from "react-native";

export default StyleSheet.create({
   container: {
      flex: 0.75,
      flexDirection: 'row',
      justifyContent: 'space-between',
   },
   item_text: {
      fontSize: 30,
      color: '#fff'
   }
})